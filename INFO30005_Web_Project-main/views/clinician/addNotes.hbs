{{#with data}}
    <header>
        <div class="top-bar">
            <div class="return">
                <a href="../{{this._id}}">
                    <i class="bx bx-chevron-left btn"></i>
                </a>
            </div>
            <div class="name">Add Notes</div>
        </div>
    </header>

    <div class="content-note">
        <p class="title-note">Enter Notes</p>

        <form
            id="form"
            action="/home/clinician_dashboard/patient/note/{{this._id}}"
            method="post"
        >

            <textarea
                name="note"
                class="note-box"
                type="text"
                placeholder="Enter...(no more than length 100)"
                maxlength="100"
            ></textarea>

            <div></div>

            <button type="submit" class="cbutton-n">Save</button>
        </form>
    </div>
{{/with}}